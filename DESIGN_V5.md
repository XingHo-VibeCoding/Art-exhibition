# DESIGN_V5.md — 动态印刷剧场视觉指引

> 试行版 · 2026-09-22
> 本文替换旧版“静态展陈 v5”，指导 GSAP 迁移后的所有视觉与动效决策。

## 0. 核心命题

网站不是一套展示作品的 UI，而是一台被观众启动的印刷机器。高级感来自工序的准确、节拍的克制和作品与容器的层级关系，不来自堆叠纹理、粒子或装饰。

## 1. 五种物理动作

| 动作 | 观看感受 | 使用位置 |
|---|---|---|
| 压 `press` | 版面合拢、急停、定稿 | 揭幕、主题定稿 |
| 刷 `squeegee` | 一次连续推进，遮蔽被刮开 | 主题换版、作品换印 |
| 揭 `lift` | 纸张慢起快走，作品离开墙面 | 展墙入场、关闭聚焦 |
| 对位 `register` | 层与层短暂错开后归零 | 聚焦切换的印正高光 |
| 收束 `reveal` | 所有多余层退场，只留终态 | 每段时间轴结尾 |

禁止把这些动作做成普通网页动画：禁止 bounce、elastic、无限循环、无原因的 3D 翻页、黑闪、白闪和大面积模糊。

## 2. 展墙构图

- 桌面端使用编辑型展墙：一张主作、两张支撑作、一个收束位；
- 主作承担第一眼重量，辅助作品承担节奏，不平均分配注意力；
- 留白是展墙的一部分，不用卡片背景填满页面；
- 卡片保持 2:3 外框，作品原比例完整呈现；
- 移动端恢复稳定单列/双列，优先保证可读和可触摸。

## 3. 动效层级

每次动作最多同时强调三层：

1. 作品层：观众最终要看的内容；
2. 纸与卡纸层：提供空间和装裱关系；
3. 墨与版号层：说明当前工序和状态。

所有动效结束后必须回到静止画面，不能把“动画正在运行”当成页面常态。

## 4. 主题系统

主题不是简单换背景色，而是一套独立的印刷条件：墙面色、卡纸色、主墨色、辅助墨色、专色和颗粒密度。主题颜色可以不同，但明度和文字对比度必须稳定。

主题专色只承担当前状态、激活导航和版号提示，不做大面积填充。作品本身不强行套滤镜。

## 5. 首屏揭幕

画面只出现纸、墨、字标三类层：

- 版面从两侧或上下压合；
- 字标在合缝时显影；
- 最后一拍是短暂压印高光；
- 总时长控制在 1.2 秒左右；
- 任意输入可跳过，跳过后必须清理时间轴和遮罩。

## 6. 主题换版

主题切换像更换一套印版：旧墙退后、新墙由刷痕推进；导航线和版号同步换色；新主题卡片以不均匀错峰进入。再次点击时，旧时间轴必须被接管，不允许排队播放。

## 7. 聚焦校样台

聚焦不是黑底弹窗，而是从展墙原位抽出一张校样：

- 使用 GSAP Flip 记录网格位置和校样台位置；
- 校样保留装裱边界、作品比例和版号信息；
- 左右切换表现为换印，不使用卡片翻转；
- 关闭时反向回到原展位和原滚动位置。

## 8. 动效节拍

默认时间参考：

| 工序 | 时长 | 节拍 |
|---|---:|---|
| 揭幕压合 | 1200ms | 60% 走位 / 10% 高光 / 30% 收束 |
| 主题换版 | 600ms | 旧版退出 / 刷痕推进 / 新版定稿 |
| 聚焦进入 | 420ms | 原位放大 / 校样稳定 |
| 作品换印 | 420ms | 遮蔽推进 / 印正 / 收束 |
| 关闭回位 | 300ms | 揭纸离场 / 回到原位 |

实际实现以 `src/motion/tokens.js` 为唯一来源，不在组件内散落时间常量。

## 9. 低动态模式

检测到 `prefers-reduced-motion: reduce` 时：

- 直接显示终态；
- 不播放刷痕、位移和过冲；
- 保留主题切换、聚焦、键盘和触摸功能；
- 不用透明度把作品隐藏掉。

## 10. 审美验收

- 动效能说清楚对应哪一道印刷工序；
- 作品的视觉重量始终高于导航和装饰；
- 页面不会因为纹理、颗粒或动画显得嘈杂；
- 每个主题有独立气质，但仍属于同一展览；
- 首屏截图不依赖动画暂停帧才能成立；
- 移动端不是桌面版的挤压残影，而是有稳定的观看顺序。

## 11. 迁移说明

旧版 `mvp/index.html` 保留为迁移基线和素材参考。后续实现以 `PRD.md`、`TECH_DESIGN.md` 和本文为准；旧版关于“零依赖、原生 WAAPI、74 条 v4 验收”的规则不再指导新代码。
