# DESIGN_V5.md — 展示系统 v5 + 作者反馈系统

> **Day 6 之后 · 2026-09-21 · 定稿**
> **上游**：`PRD.md` v4（74 条验收）／`research.md`（Day 3）／`mvp/design-v5-proposal.md`（提案稿）
> **性质**：本文档是 v5 的正式蓝图。`PRD.md` 的 §6 与相关 AC 条款将在 **S1 阶段**按 §7 清单修订；修订前，两者冲突处以本文档为准。
> **一句话**：把「容器自带材质」换成「容器中性、环境随作品」，显影语法由「错版对位」换成「**刷子刷出**」，印刷从世界观降级为六种手感之一。

---

## 0. 这一版推翻/保留了什么

| | v4 | v5 |
|---|---|---|
| 世界观 | **丝网印刷**（容器自带网点/错版/规矩线/金属版号） | **展陈**（容器中性，材质只是作品的属性） |
| 分类依据 | 隐含按材质 | **按主题**（材质降为属性字段） |
| 显影语法 | 错版合拢（破坏性：中途颜色是错的） | **刷子刷出**（累积性：颜色一直是对的） |
| 环境色 | 六套预设印版，全站统一 | **随作品生成**，每张一套 |
| 互动 | §4.2 A3 列为「永久不做」 | **作者反馈系统**（点赞 + 一句话感想） |
| 保留 | 2:3 竖版 / 零裁切 / 60-30-10 / 时间轴 60-30-10 / 节拍 M0–M4 | **全部保留** |

---

## 1. 已定决策（九条）

| # | 决策项 | 选择 | 日期 |
|---|---|---|---|
| D1 | 展示方向 | 展陈骨架 + 环境随作品 | 09-21 |
| D2 | 互动级别 | 点赞 + 一句话感想（≤50 字、无回复） | 09-21 |
| D3 | 点赞门槛 | 匿名 + 浏览器本地去重 | 09-21 |
| D4 | 热度可见性 | 不显示数字，但影响排序 | 09-21 |
| **D5** | **分类方式** | **主题优先；材质降级为属性字段** | 09-21 |
| **D6** | **显影语法** | **刷子刷出（原「错版对位」作废）** | 09-21 |
| **D7** | **排序策略** | **方案甲：位次不动，热度只改入场先后** | 09-21 |
| **D8** | **观众地域** | **主要在国内** | 09-21 |
| **D9** | **后端平台** | **待定**（S1 不依赖后端，数据层走 adapter） | 09-21 |

---

## 2. 分类结构（D5 落地）

### 2.1 三层，只有第一层对外

| 层 | 用什么 | 观众可见 |
|---|---|---|
| 一级导航 | **主题**（六个系列） | ✅ 唯一入口 |
| 二级 | 作品（每张卡） | ✅ |
| 属性字段 | 材质 `material` | ❌ 不可见 |

### 2.2 为什么主题优先

**主题是"一眼能懂"的分类；材质是"需要解释"的分类。** 按作者的验收线——抽象 ≠ 高级，看不懂的秩序就是没秩序——材质分类属于后者。观众不会想"这是一张数字插画"，他只会想"这是塔罗那组里的那张"。

**收益**：分类从「六主题 × 六材质」的二维表塌缩成「六主题」一条线。以后新增多少种风格的图，结构都不变。

### 2.3 主题清单（S1 起用）

`Now You See Me` ／ `超现实主义` ／ `盗梦空间` ／ `Steins;Gate` ／ `扑克牌` ／ `塔罗牌`

### 2.4 材质字段的两项暗职能

```
material: print | film | photo | collage | digital | gameui
```

1. **决定刷子的手感**（见 §3.3）—— 观众只觉得"每张的手感不太一样"，说不出为什么
2. **铭牌上加一个极小的工艺标记**（可选，S1 可先不做）

> 材质不参与导航、不参与筛选、不显示为文字。它是给实现层用的字段，不是给观众看的标签。

---

## 3. 显影语法 · 「刷子刷出」（D6 落地）

### 3.1 为什么换掉「错版对位」

| | 错版重影（v4） | 刷子刷出（v5） |
|---|---|---|
| 显影中途的画面状态 | **颜色是错的**（重影 / 彩边） | **颜色一直是对的**，只是位置还没到 |
| 对柔和作品（水彩 / 星空 / 柔光） | 像显像管故障 | 无副作用 |
| 本质 | **破坏性**显影 —— 先错再对 | **累积性**显影 —— 从无到有 |

> **一句话：错版让你看到"还没对的颜色"，刷子让你看到"还没到的位置"。前者污染画面，后者只是遮蔽。**

这解释了「影响观感」——不是参数没调好，是**机制本身**在柔和作品上必然出错。

**附带的巧合**：刷子（squeegee）本就是 v4 已存在的曲线 `--c-squeegee`（刮墨匀速）。**v5 没有新造机制，只是把一条边角料提升为主语法。** 原「对位」语法整个退场，不再保留。

### 3.2 语法四要素（恒定，不随材质变）

| 要素 | 规格 |
|---|---|
| **动作** | 一次连续的手部动作，从无到有，**只此一次** |
| **方向** | 单方向推进（左→右 或 上→下），可带轻微斜角 |
| **边缘** | 笔触毛边 —— 不是硬边，更不是重影 |
| **结束** | 刷满即定，**之后无残余动作**（沿用 §6.5 的 10% 高光纪律） |

**节拍骨架沿用 §6.5，不重新发明**：走位 60%（匀速）／收束 30%（慢起快走）／高光 10%（快起急停，仅一次）。

### 3.3 六种手感（材质 = 方言）

| 材质 | 工具 | 边缘 | 推进 |
|---|---|---|---|
| `print` 印刷 | 硬胶刮刀 | 锐利、轻微墨溢 | 匀速 |
| `digital` 数字插画 | 软毛笔 | 晕开 | 慢起快走 |
| `film` 胶片 | 片窗 | 硬边、一格一格 | 阶跃 |
| `photo` 摄影 | 大排笔 | 极软 | 匀速 |
| `collage` 拼贴 | 短促多下 | 块状参差 | 不规则 |
| `gameui` 游戏界面 | 斜切色块 | 硬边斜角 | 快 |

**六种都是"刷"，只是手上的工具不同。** 语法不变，方言可变——"一眼看出不一样"仍然成立。

### 3.4 实现要点

- **手法**：分段错峰的 mask 揭示（示意实现为 9 段 × 20px、延迟递增 0.12s；真实卡片按尺寸换算）
- **禁用 filter**（`feTurbulence` / `feDisplacementMap`）做毛边 —— 性能与兼容风险，改用分段矩形制造参差
- **关键性质**：mask 只改"遮蔽"，**不改动画面像素** → 对任何色彩风格都安全
- **S5 可升级**：Canvas 笔刷纹理，做真正的边缘毛糙
- `prefers-reduced-motion` → 直接呈现终态，不播放

---

## 4. 三层结构（恒定 / 变量 / 语法）

### 4.1 恒定层 · 展陈骨架（永不随作品变）

| 项 | 规格 | 来源 |
|---|---|---|
| 基本单元 | 统一外框 **2:3** 竖版 | 沿用 AC-26 |
| 装裱 | 作品按原比例完整放入，四周留卡纸（mat），**零裁切** | `mvp/fate-card` 已验证 |
| 卡纸宽度 | 外框短边的 **6%** | 新增 |
| 铭牌 | 卡纸下沿居中，三级字号（作品名 > 系列 > 编号） | 沿用 AC-74 |
| 列数 | 4 / 3 / 2 列（≥1280 / 768–1279 / <768） | 沿用 AC-27 |
| 卡片间距 | `(可用宽 − 卡片宽之和) ÷ (列数 + 1)`，≥ 卡片宽 5% | 沿用 AC-72 |
| 页面留白 | 左右 8%（桌面）/ 6%（手机） | 沿用 AC-73 |

> 美术馆不要求展品同风格，只要求展陈系统统一。这一层负责把 74 张风格各异的作品**同等郑重地对待**。

### 4.2 变量层 · 环境色随作品（每张一套）

**三步，只在聚焦视图触发**（不在网格里跑 74 次）：

1. **取色**：作品降采样到 32×32 → 算明度中位数 `L` 与色相直方图峰值 `H`
2. **受控生成**（关键：不直接用原图颜色）
   - 明度落入窄带：深带 `[0.18, 0.32]` 或 浅带 `[0.82, 0.92]`
   - 饱和度压到 `S ≤ 0.15`（墙不抢画）
   - 色相取 `H`
3. **广播**：写入 `--wall` / `--mat` / `--ink`，背景、卡纸、阴影由它派生

**兜底**：取色失败（大图超时 / 跨域）→ 回退中性灰白，不报错、不留白。

**★ 顺带结清的老债**：明度被锁进窄带 → **AC-46「两块大面积色域明度反差 ≤3:1」与六套主题的冲突自动消失**，不必再为每套主题单独调明度。

### 4.3 语法层

见 §3。

---

## 5. 作者反馈系统

### 5.1 定位（它决定后面所有选择）

PRD §4.2 A3 把「用户系统 / 评论 / 社交」列为永久不做，理由是「展馆不需要留言簿才能看画」。**这个理由今天仍然成立——观众依然不需要它。**

本次需求是**作者侧**的：「让我知道哪张被喜欢」。两个问题不同，导出不同设计：

- ❌ **不做**：点赞排行榜、热门推荐、社交分享 —— 会把观众的眼睛从作品引到数字上
- ✅ **做**：「安静的热度」—— 默认不显示数字，只在作者视图可见

### 5.2 功能与数据

| 级别 | 功能 | 观众看到 | 门槛 |
|---|---|---|---|
| L1 | 点赞 | 一颗心，点亮 / 再点熄灭 | 零门槛，匿名 |
| L2 | 一句话感想 | 昵称（可留空）+ ≤50 字，倒序列于作品下方 | 零门槛 |
| — | 作者视图 | 每张的点赞数 + 感想清单 | 仅作者 |

```
works  (id, series, seq, title, material)
likes  (id, work_id, visitor_id, created_at)   -- 唯一索引 (work_id, visitor_id)
notes  (id, work_id, visitor_id, nickname, body, status, created_at)  -- status: visible|hidden
```

### 5.3 接口（4 个端点）

| 方法 | 路径 | 作用 |
|---|---|---|
| `POST` | `/api/like` | `{workId, visitorId}` → `{liked, count}` |
| `GET` | `/api/stats` | 全部作品的点赞数与感想数（供排序用） |
| `POST` | `/api/note` | `{workId, visitorId, nickname, body}` → `{ok}` |
| `GET` | `/api/notes?workId=` | 某作品的可见感想 |

**服务端必做的三件事**：`body` 强校验 ≤50 字；同 `visitorId` 对同一 `workId` 点赞 1 次、感想 1 条；同 IP 每分钟 ≤10 次写操作。

### 5.4 排序 · 方案甲（D7 落地）

**作品的位次永远按作者的策展顺序（`works.seq`），热度只影响入场先后** —— 被喜欢的先入场。

> 这是全案最需要克制的一处。美术馆不按参观人数重排画，但它可以有节奏。
> **方案甲把"热度"翻译成了「节奏」，而不是「等级」**：观众说不出来哪里不一样，只觉得这面墙有生气。秩序零损失，且不用新增机制——它就是 §6.5 时间轴的延长线。

（方案乙「热度最多提前一位」已评估，**不采用**：它改写作者排的策展顺序，代价大于收益。）

---

## 6. 后端选型（D8 / D9 落地）

### 6.1 现状风险（D8 = 主要在国内）

| 环节 | 现状 | 国内可用性 |
|---|---|---|
| 前端 | `github.io` | ⚠️ 时好时坏（DNS 污染 / 偶发不可达） |
| 后端（原倾向 CF Workers） | `*.workers.dev` | ❌ 更差 → **降级为「仅海外」** |

**所以真正的问题不是"后端选哪个"，是"这个站的正式发布地址在哪"。** GitHub Pages 可以继续当开发期预览，但国内观众要稳定打开，只有一条路：**自有域名 + ICP 备案**（7–20 天，是目前唯一需要提前排队的事）。

### 6.2 候选（国内优先）

| 方案 | 覆盖 | 匿名可赞 | 免费 | 备注 |
|---|---|---|---|---|
| **腾讯云开发 CloudBase** | 静态托管 + 云数据库 + 匿名登录 | ✅ | 有免费额度 | 一个产品覆盖 §5.3 全部端点；需实名 |
| 腾讯云 EdgeOne Pages | 静态托管（国内节点） | — | 有免费额度 | 前端方案，需自有域名 |
| 阿里云函数计算 + OSS | 计算 + 存储 | ✅ | 有免费额度 | 需实名 |
| Cloudflare Workers + D1 | 全套 | ✅ | ✅ | **仅海外**，国内不稳 |
| Giscus | 评论 | ✅ | ✅ | **须 GitHub 账号** → 违反 D3，出局 |

### 6.3 分步策略（★ 后端待定不阻塞任何事）

- **S1 完全不需要后端** —— 展示系统改造是纯前端
- 数据层先写成 **adapter**（本地 mock 实现同一组接口），后端定了换实现，前端代码不动
- **唯一建议提前启动的事**：自有域名 + ICP 备案，与 S1 并行推进

---

## 7. 并入 PRD 的条款清单（S1 阶段执行）

| 位置 | 现状 | 改成 |
|---|---|---|
| §2.1 一句话定位 | 「2:3 竖版丝网印刷卡片 / 海报」 | 去掉「丝网印刷」，改为「2:3 竖版艺术作品」 |
| §3 功能范围 | 用户系统/评论/社交「明确不做」 | 移入 MVP，定位「作者反馈系统」 |
| §4.2 A3 | 标注「永久不做」 | **翻案**，写明理由（作者侧需求 ≠ 观众侧需求） |
| §6.1 主题系统 | 六套预设印版（三版 60-30-10） | 改为「**环境色随作品生成**」；六套降级为可选策展预设 |
| §6.4 P1–P6 | 印刷物证**全站强制** | 降级为「`material = print` 的方言」 |
| §6.5.4 四条曲线 | 印厂工序曲线 | 保留节拍骨架，曲线名改为**材质中性**的说法 |
| §6.5 显影动作 | 错版合拢 | **改为刷子刷出**（§3） |
| AC-38 | 断网可用、零外部依赖 | **分层**：浏览层零依赖；互动层允许失败降级 |
| AC-42 | 无错图无空缺 | 不变（作品须与数据表一一对应） |
| AC-43/44/45/47/48/49/50/52/55/66 | 三版配比 / 金属只走线脚 / 网点 / 规矩线 | **限定适用范围**：「当作品 `material = print` 时适用」 |
| 新增章节 | — | §11 作者反馈系统 / §12 展示系统 v5 |
| 新增 AC | — | 约 12–15 条（匿名去重、长度校验、离线降级、热度只改入场等） |

**净变化预估**：74 条 → **约 86–89 条**。

---

## 8. 迁移路径

| 阶段 | 内容 | 动 PRD | 前置 |
|---|---|---|---|
| **S1** | 展示系统 v5：展陈骨架 + 环境色随作品 + 刷子语法（先做 `print` 与 `digital` 两种手感） | 是（§6 全节） | 本文档 |
| S2 | 接入后端：点赞 + 方案甲排序 | 是（新增 §11） | 后端选型 |
| S3 | 一句话感想 + 作者可隐藏 | 否 | S2 上线 |
| S4 | 作者视图（热度面板） | 否 | S2 |
| S5 | 补齐其余四种手感（`film` / `photo` / `collage` / `gameui`） | 否 | S1 验证通过 |

**S1 的验证法**：**只做一张卡**（沿用 `mvp/fate-card` 的孤立验证法），用**一张柔和 + 一张强烈**各试一次。两张都站得住，才铺开 74 张。

---

## 9. 待确认（3 项）

| # | 待确认 | 建议 |
|---|---|---|
| 1 | PRD 是「S1 阶段改」还是「现在就改」？ | S1 阶段改——AC 是验收契约，实现之前改写会出现「文档达标、代码没有」的错位。现在只在 PRD 顶部留了指引 |
| 2 | 是否已有自有域名？要不要现在启动 ICP 备案？ | 国内稳定访问的唯一路径，建议尽早 |
| 3 | S1 验证用哪两张图？ | 一张柔和（新海诚风格）+ 一张强烈（惊天魔盗团 P5R） |

---

*最后更新：2026-09-21 · 定稿 · 取代 `mvp/design-v5-proposal.md`*
