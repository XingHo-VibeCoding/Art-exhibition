# AGENTS.md — 项目规则

> **这份文件有两重身份：**
> 1. **合作契约** —— 第一至七节，是主人写下的协作规则，字句不改、逐条执行。
> 2. **项目档案与底座** —— 第零节 + 附录，记录这是什么项目、什么环境、什么工程规范。
>
> 每次开新会话，先读第零节定位，再读第一至七节立规矩，最后按附录执行。

---

## 第零节 · 项目档案

> ⚠️ 本仓库是 **public**，本节所有人可见。因此只写事实、不写私人信息：姓名、学校、本机绝对路径一律用占位符，真实值由当次会话的上下文补全，不落盘。

- **用户**：`<你的名字>`（`<你的学校>`，本科在读）
- **项目**：Vibe coding —— 21 天 vibe coding 实操学习计划
- **工作区根目录**：`<项目根目录>`
- **当前阶段**：Day 1（第 1 周）· 环境搭建与规则落地
- **技术栈**：

| 项 | 版本 / 位置 |
|---|---|
| 操作系统 | Windows (win32) |
| Node.js | v24.21.0 @ `<Node 安装目录>` |
| npm | 11.19.0，源 = `https://registry.npmmirror.com/` |
| Git | 2.55.0.windows.3 |
| AI 环境 | WorkBuddy |

- **节奏总纲**：理解 > 完成。

---

## 一、任务边界（最重要）

1. 收到我的任务清单后，**这份清单就是今天唯一的任务范围**，只做清单里写的事。
2. 不做清单以外的工作：不建多余的文件、不写清单没提的功能、不顺手优化已有代码。你觉得可以改得更好，先跟我说一句，等我点头。
3. 不提前做以后的天数。只有我把某天的清单发给你、并明确说「现在开始做 Day X」，才允许动那天。
4. 拿不准是否在范围内，先问我，不要猜。

## 二、交互式推进

1. 收到清单后，先只给我今天的执行计划：按什么顺序做、每步怎么做、哪些要我自己动手。**写完停下，等我确认。**
2. 确认后**一次只做一个步骤**，绝不连着往下做。
3. 每步做完立刻停下，告诉我三件事：做了什么、改了哪些文件、怎么验证（附我能亲眼确认的结果）。
4. 然后**停下等我回复「进入下一板块」**。我没说这四个字，你就不要继续。
5. 「进入下一板块」只对当前这一步有效，不代表可以一路做到收尾。

## 三、人机分工

1. 需要我人工操作的（注册账号、点按钮、装软件、截图等）：不要替我做，也不要假装已完成。只告诉我：这步在干什么、一二三步怎么做、做完我该看到什么、容易踩什么坑，然后停下等我。
2. 需要你生成的内容（写文档、建文件、写代码）：**等我说了再做**，不要在我刚说「开始吧」时就把整天的东西生成出来。
3. 需要拍板的地方（选方案、起名字、定风格），把选项和各自代价列给我选，不要自己决定。
4. 验证结果要我能亲眼确认（页面能打开、文件能看到、命令有输出），不接受「已经完成了」这种口头结论。

## 四、讲清楚环节（我是来学的）

1. 每个步骤开始前，先告诉我：它在整个作品里是什么位置、为什么现在做、做完会变成什么样。
2. 每步用「做了什么 → 结果 → 为什么」讲明白，不要只给结果。
3. 我不懂的概念（命令、报错、术语、配置），一两句话解释清楚，不要默认我会。
4. 报错时先告诉我错在哪、可能什么原因、打算怎么修，我同意后再动手。不要输出教程式长文。

## 五、Git 与提交

1. 我授权你代为执行 git 提交和推送，但**提交前必须先列出**本次会改动哪些文件、每个文件属于哪天的任务，等我确认后再提交。
2. commit 信息格式：`Day X｜一句话说明`。
3. 密钥、`.env`、数据库连接串永远不进代码、不进提交。
4. 一天的任务全部做完、我核对完文件清单之后才提交，中途不要边做边提交。

## 六、当日收尾

1. 我说「今天做完了」之后，请你逐条对照清单里的「完成标准」，输出一张表：完成标准 / 状态（已完成·部分完成·未完成）/ 证据（文件、页面、命令输出）。
2. 核对必须诚实，没做完、做了一半、跳过没做的，直接标出来并说明原因，不要为了凑「全部完成」而模糊处理。
3. 有未完成项时，告诉我还差什么、接不接着做，**由我决定**，不要自己往下做。
4. 全部完成时，按清单的「收尾指令」整理当天成果，提醒我该交哪张截图、该在群里回什么。

## 七、出问题的时候

1. 我要求撤销时，用安全回退（`git revert`，保留历史），禁止 `git reset --hard` 和强制推送。
2. 回退前先和我确认恢复到哪个提交，回退后列出当前状态让我核对。
3. 同一个操作反复失败时停下来报告，说清楚试了什么、报什么错、你判断的原因，不要循环重试。

---

## 附录 · 工程规范（技术层补充）

### 代码规范

- **命名**：变量与函数用 `camelCase`，常量用 `UPPER_SNAKE_CASE`
- **缩进**：2 个空格
- **注释**：关键逻辑写中文注释，说明「为什么这么做」而不是「这行在做什么」
- **文件组织**：按功能分目录，不把所有文件堆在根目录

### 技术层禁忌

- ❌ 不使用未经主人确认的第三方依赖
- ❌ 不删除工作区内的任何文件（除非主人明确要求）

---

## 我的个人规则

> 追加于 Day 6（2026-09-21）。第一至七节是协作契约，本节是为这个项目补的三条实操规则。
> 每条后面写明**它防的是哪种情况**——规则不是装饰，是「以前摔过的地方立个路牌」。

### A · 文件存放习惯

- 根目录只放 **5 份文字档案**（`AGENTS.md` / `PRD.md` / `research.md` / `TECH_DESIGN.md` / `DESIGN_V5.md`）与站点入口 `index.html`。
- 实验性页面与素材放 `mvp/`；参考文献与取证截图放 `_ref/`（**不进仓库**）。
- 实验品**定稿后搬出 `mvp/`**，升格为根目录档案（如 `DESIGN_V5.md`）—— 档案是对外的，实验品是给自己看的。
- 新建文件前先说明它属于哪一类、放哪里，不要顺手丢在根目录。

**防的是**：根目录长草。文件一多就分不清新旧，最后连「哪一版才是现在的网站」都答不上来。

### B · 语言偏好

- 对话、代码注释、文档一律用**中文**。
- 站点界面文案一律用**英文**，且全站 ≤ **40 个英文单词**（作品名不计入）。
- 两套语言不互串：不把英文标语写进代码注释，也不把中文写进页面。

**防的是**：两套语言混着用。PRD 一边要求中文注释、一边要求英文界面，不写死就必然串味。

### C · 验证习惯

- 任何视觉改动，必须给一条**能亲眼确认**的证据：无头 Chrome 截图路径 + 文件体积判据（正常 110–190KB，失败约 3.6KB）。
- 不接受「已完成」「应该没问题」这类口头结论。
- 验证失败就直说失败，不用「基本符合」模糊处理。

**防的是**：「看起来对、实际全错」。牌面上已经翻过两次车——图形被压在墨层底下、浅色被 multiply 吃成脏棕，两次都是「代码没错、画面全错」。

---

_最后更新：2026-09-21（Day 6 · 追加个人规则 A/B/C；第零节脱敏；A 条随 `DESIGN_V5.md` 升格为档案同步更新）_
