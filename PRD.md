# PRD.md — Art Exhibition

> **Day 4** · 2026-09-19 · 五步工作流第 3 步「定义」
> **上游**：`research.md`（Day 3 需求研究）
> **下游**：Day 5 技术选型 → Day 7 MVP 实现
> **仓库**：`github.com/XingHo-VibeCoding/Art-exhibition`
> **本文档语言**：中文（便于作者与技术评审阅读）
> **界面文案语言**：英文（面向公众展站，见 §6.3）
> **版本**：**v4** —— v1 首稿 → v2 按作者审美知识库升级 → v3 收口 60-30-10 并扩到时间轴 → v4 自检后修订
> **v2 变更**：§6 整节重写（三版系统 + 金属黏合剂 + 印刷物证）；§5.2 主题切换由「平滑渐变」改为「换版」；§10 新增 10.7 视觉系统验收 13 条
> **v3 变更**：① **收回 v2 的「破 60-30-10」**——作者指令「还是用 60-30-10 面积配比」，第四色降为不占配比的工艺副产品；② **新增 §6.5 动效规格**，把 60-30-10 从「面积」扩到「时间」，立 6 条动效定律（M0–M4 + 四曲线）；③ §5.1/5.2/5.3 各补一行「节拍」；④ §10 新增 10.8 动效验收 14 条
> **v4 变更**（2026-09-19 · 自检后修订）：① §8 全部待确认项转为**已决策记录**（作者指令「都按推荐的来」）；② 修复自检发现 **6 处「必须修改」**（AC-07 与 v3「换版」冲突 ／ AC-43 残留「点睛色配比」 ／ 「对比度 ≥4.5:1」与「亮度比 ≤3:1」作用对象混淆 ／ M1 与全画幅换版冲突 ／ AC-39 与 AC-69 口径矛盾 ／ AC-15 主题列举错漏）；③ 补 **5 条漏项验收** AC-70…AC-74；④ 验收标准 **69 → 74 条**
> **约束**：本文档不含代码，只描述「做成什么样算完成」

> **⚠️ v5 修订预告（2026-09-21）**：展示系统与互动范围已在 `DESIGN_V5.md` 定稿 —— 显影语法由「错版对位」改为「**刷子刷出**」、分类改为**按主题**（材质降为属性字段）、新增**作者反馈系统**（点赞 + 一句话感想 + 方案甲排序）。
> 本文档 §6 及下列清单中的 AC 条款，将在 **S1 阶段**按 `DESIGN_V5.md` §7 的清单修订；修订完成前，两者冲突处以 `DESIGN_V5.md` 为准。
> 此处只留指引，**不预先改写验收标准** —— AC 是验收契约，在实现之前改写会造成「文档达标、代码没有」的错位。

---

## 一、这份文档是什么

`research.md` 回答了 **「想要什么」**；这份 PRD 回答 **「做成什么样算完成」**。

差别在于**可验收**。研究可以写「动效要流畅」，PRD 必须写「悬停帧率 ≥ 55fps，且首屏不超过 3 种颜色」。前一句谁都能写，后一句才能拿去做验收。

**本文档的读者有三个：**
| 读者 | 关心什么 |
|---|---|
| 作者（你） | 这一版到底要做成什么，砍了什么 |
| 实现者（Day 7 的 AI） | 每个功能的确切行为与边界 |
| 验收者（你 + AI 自检） | 逐条勾选的判据 |

---

## 二、产品背景与目标

### 2.1 一句话定位

> **一个把过往 2:3 竖版丝网印刷卡片 / 海报，按「系列」组织起来的在线展览页。**

**品类定位：流行文化收藏卡展。不是美术馆。**（依据 `research.md` §一）

### 2.2 要解决的三个问题

| # | 问题 | 现状 |
|---|---|---|
| 1 | **散** | 作品只在本地文件夹里，没有统一入口 |
| 2 | **乱** | 作品天然成系列，但没有地方体现这种结构 |
| 3 | **平** | 静态图无法传达丝网印刷「错版 / 墨色位移」的物理性质 |

### 2.3 目标用户

| 项 | 内容 |
|---|---|
| **主要** | 对艺术感兴趣的人群，尤其是影视 / 游戏视觉爱好者、独立创作者的同好 |
| **次要** | 未来想了解作者做过什么的人（合作方、关注者） |
| **他们的场景** | 点开一个链接，几秒钟内判断「这里值不值得翻下去」 |
| **他们的期待** | 感官上：有质感、有仪式感；信息上：能看懂这是什么、有多少、怎么翻 |

> 📌 目标用户「对艺术感兴趣」意味着：**他们见过好的**。粗糙的动效比没有动效更伤人。这也是本版把动效定为 MVP 必需项的原因。

### 2.4 本期目标（可衡量）

| # | 目标 | 衡量方式 |
|---|---|---|
| G1 | 作品有统一入口，能被完整翻完 | 从揭幕到看完最后一个系列最后一张，全程无死路 |
| G2 | 系列结构可见 | 任一时刻，用户能说出「现在看的是哪个系列、共几张、这是第几张」 |
| G3 | 丝网印刷的物理性质被「演出来」 | 至少 1 处动效直接来自印刷语言（错版对位），而非通用网页特效 |

**本期不追求**：流量、留存、转化。这是一个展示柜，不是产品。

---

## 三、功能范围总览

| 层级 | 内容 |
|---|---|
| **✅ MVP 必做** | F-01 揭幕 · F-02 主题切换 · F-03 作品切换（此三项为作者指定）<br>F-04 展场 · F-05 聚焦（此两项为前者的必要支撑，见 §5） |
| **🚫 明确不做** | 商城售卖 · 教程/资源导航 · 用户系统/评论/社交 · 后台上传 · 多语言 · 搜索筛选 · 关于页 |
| **⏳ 后续** | 全站页面转场 · 毛玻璃/融球/螺旋扩散 · 音波/光标特效 · 动画库引入 · 移动端专属动效 |

详见 §4（砍掉什么）与 §7（后续）。

---

## 四、★ 本期砍掉了什么，为什么

> 本节是 Day 4 的核心产出，对应今日「要掌握」的题。

### 4.1 先分清两把刀

`research.md` §六已经砍过一批了。但**那一刀和这一刀不是同一种刀**：

| | 研究阶段的砍 | PRD 阶段的砍（本节） |
|---|---|---|
| **性质** | **品类性** | **工期性** |
| **回答** | 这个展馆该不该有咖啡馆 | 开馆那天先开哪两个厅 |
| **依据** | 它不是这个品类该有的东西 | 它该有，但一个版本做不完 |
| **例子** | 商城、用户系统、教程门户 | 全站转场、搜索、关于页 |
| **判断期** | 永久 | 下个版本可翻案 |

**为什么必须分清**：研究砍完之后，剩下的每一项**单独看都正当**——揭幕正当、总览正当、详情正当、动效正当。今天要在这一堆全能成立的东西里，砍出**一个做得完的版本**。如果只是把研究清单抄一遍，等于没砍。

### 4.2 砍掉的清单

#### A. 永久不做（品类性的，下版本也不翻案）

| # | 砍掉的功能 | 为什么砍它 |
|---|---|---|
| A1 | **商城 / 限量版画售卖** | Salvat 卖版画是他的商业模式，作者现阶段不卖。展览一旦挂上价格，观众看作品的眼睛就变成了看货的眼睛。 |
| A2 | **教程 / 资源导航 / 字体库 / 图标库** | 那是门户站的逻辑（加法）。本站是展览（减法）。`ignoredone.space` 学来的三个可学点里**不含这一条**——它是该站「不能学」清单的第一项。 |
| A3 | **用户系统 / 评论 / 社交分享** | MVP 不需要。展馆不需要会员卡，也不需要留言簿才能看画。 |
| A4 | **后台上传 / 内容管理界面** | 作品增删频率极低，**手工维护即可**。为一个季度改一次的事造一套后台，成本远超收益。 |
| A5 | **多语言切换** | 界面文案已定为英文（§6.3），单一语言覆盖全部目标用户。 |
| A6 | **关于页 / 作者简介** | ★ 直接踩在 `research.md` 的核心发现上：三个顶级艺术站（Sheehy / Hobbs / Salvat）**首屏几乎没有一句介绍性文案**。作品自己会介绍自己。加在它前面的那段话，多半只是在延后观众见到它的时间。 |

#### B. 本期不做（工期性的，进 §7 后续清单）

| # | 砍掉的功能 | 为什么砍它 |
|---|---|---|
| B1 | **搜索 / 筛选** | 搜索是给「多」准备的工具。本站是 6 个系列规模，人眼一扫就见底。加搜索等于给一居室装中央空调。 |
| B2 | **全站页面转场** | 本版为**单页结构**（§5.2），跨页跳转极少，转场收益低。`research.md` 6.2 原定为「看情况：桌面不掉帧且确有跨页跳转才做」——本期前提不成立，故降级。 |
| B3 | **背景毛玻璃 / 融球 / 螺旋扩散** | ★ **位置被占了**。本版新增的「背景随主题渐变」（F-02）已经定义了背景的角色：**背景是纸，它只负责换色**。再叠一层融球/毛玻璃，背景就从「纸」变成了「另一个主角」，与 §6.2 构图的「作品优先」直接冲突。 |
| B4 | **音波可视化 / 光标特效** | 同类问题：干扰视线。`research.md` 的统一红线是「任何动效，只要让观众第一眼看的不是卡片，就砍掉」。 |
| B5 | **引入 JS 动画库 / WebGL / CDN** | `research.md` §八记下的张力：**单文件零依赖 ↔ 重动效天生打架**。本版所有动效均由 CSS / SVG 原生能力可及，不必付「断网打不开」这个代价。判断权留在 Day 5。 |
| B6 | **移动端专属动效** | 移动端本版只保「基础可用」（§6.2），不做专属编排。理由：实现成本高，且移动端是次要访问场景。 |

### 4.3 一句话回答「今天砍了什么」

> **砍掉的全部是「让站更像一个产品」的功能，留下的是「让作品更像一场展」的功能。**

搜索、关于页、转场、背景特效——这四样都在增加**站的存在感**。而本站要的恰恰相反：**站要退后，作品要上前。**

---

## 五、MVP 功能规格

### 5.0 页面结构（信息架构）

```
        ┌─────────────────────────────┐
        │  A · 揭幕  Unveiling        │   首次进入，≤1.2s，可跳过
        └──────────────┬──────────────┘
                       ↓
        ┌─────────────────────────────┐
        │  B · 展场  The Floor        │   系列导航（=主题切换器）+ 作品网格
        └──────────────┬──────────────┘
                       ↓ 点一张卡
        ┌─────────────────────────────┐
        │  C · 聚焦  Focus            │   大图 + 上一张/下一张 + 编号
        └──────────────┬──────────────┘
                       ↑ 关闭 / Esc
                       └──── 回到 B（停在同一位置）
```

三屏，单页，无跨页跳转。**主题切换器就是系列导航器**（见 F-02）——一套控件干两件事，避免出现两组导航。

### 5.1 F-01 · 揭幕（Unveiling）｜作者指定

对应 `research.md` 5.1 入口策略 C。

| 项 | 内容 |
|---|---|
| **用户动作** | 打开链接。**什么都不用做。** |
| **可观察结果** | ① 屏幕上出现两道错位的色块（墨黑 / 朱砂），向中线收拢<br>② 合拢的一瞬，字标 `ART EXHIBITION` 从两块的缝隙中「压印」显影<br>③ 字标向上移出，展场自下而上浮现<br>④ 全程 ≤ 1.2 秒，结束时不需任何操作即可开始浏览 |
| **★ 节拍** | **720ms 走位 → 120ms 高光 → 360ms 收束**（60 / 10 / 30）。高光 = 字标压印显影那一瞬，全场唯一的高饱和事件，之后一切静止。详见 §6.5.5 |
| **跳过** | 揭幕期间**任意**点击 / 按键 / 滚动 → 立即跳到终态，不播残余动画 |
| **画面上只能有三样东西** | ① 两道色块 ② 一个字标 ③ 一条细横线。**不许多任何元素。** |
| **明令禁止** | ❌ 转圈进度条 ❌ 百分比数字 ❌ 进度条 ❌ 音效 ❌ 任何装饰性图形 |

**为什么这样定义「高端艺术感」**：
> 转圈的圈是在说「还没好」，是对不起观众的话。**印版对位**是在说「要开始了」，是邀请。
> 高端不是加东西加出来的，是**删到只剩一个动作**。全场只有一个动作——**对位**。

**约束来源**：`research.md` 5.1 三条（≤1.2s / 可跳过 / 揭幕而非等待）全部保留，未松动。

### 5.2 F-02 · 主题切换（Theme Shift）｜作者指定

> ⚠️ 本节是作者新增需求，`research.md` 中无对应条目。已在 §8 列为待确认项。

| 项 | 内容 |
|---|---|
| **用户动作** | 点击顶部的任一系列名（英文，如 `LIMBO`） |
| **可观察结果** | ① **背景是「换版」，不是平滑渐变**：新主题的墨以**半调网点**从画面上缘向下压印（网点由稀到密），旧墨被刮走 —— 全程 600ms<br>② 主版 / 辅版 / 金属专版三块版**依次**到位（不同时），像三块版依次过机<br>③ 卡片**先退出、再入位**：360ms 起旧卡退出，420ms 起新卡按**非均匀错峰**入位，**540ms 前全部到位**（整个换版不超出 600ms，见 AC-65）<br>④ 被点击的系列名进入激活态，下划线用**金属专版色**<br>⑤ 地址栏追加锚点 `#limbo` |
| **★ 节拍** | **360ms 走位 → 180ms 收束 → 60ms 高光**（60 / 30 / 10）。三块版依次过机，**金属最后**——它是全场唯一允许的高饱和，且只亮一处。详见 §6.5.5 |
| **状态保持** | 刷新页面后停留在同一主题 |
| **约束** | 全程文字对比度 ≥ 4.5:1（换版途中也不能有一帧读不清） |
| **★ 关键区分** | **背景「换版」，内容「换印」——两道不同的工序，不能混。**<br>· 背景是纸上的墨底 → **换版**：旧墨刮走、新墨压下（网点由稀到密）<br>· 内容是一张具体的印张 → **换印**：揭掉旧的、放上新的（位移 + 淡出淡入）<br>**为什么不用屏幕式平滑渐变**：丝网印刷的天然弱项就是细腻渐变，它只能靠半调网点模拟（`知识卡-印刷`）。改用平滑色相渐变，等于当场承认「这不是印刷品」。 |

### 5.3 F-03 · 作品切换（Plate Change）｜作者指定

> 「用户切换图片的华丽效果」在此落实。**本项吸收了 `research.md` 5.3 已拍板的「卡片 hover 错版分离」**——两者是同一套印刷语言的两面：主动切换（重印）与静置悬停（微错位）。若不同意此合并，见 §8。
> **命名消歧**：本节的「换版」指*换一张新印张*（重印）；§5.2 的「换版」指*换那三块印版*。同一个词，一个换的是**结果**，一个换的是**原料**——故两处英文名刻意不同（`Misregistration Swap` vs `Theme Shift`）。

| 项 | 内容 |
|---|---|
| **触发方式** | ① 点击左右箭头 ② 键盘 `←` / `→` ③ 移动端横向滑动 |
| **用户动作** | 在聚焦视图中按下 `→`（或点右箭头） |
| **可观察结果** | ① 旧图**墨色层**向一侧移出 8px 并淡出<br>② 新图**墨色层**从相反方向 8px 处移入并归位<br>③ 收束末尾，两层的错位量与主版重合，画面"印正"<br>④ 右下角编号翻页：`No. 003` → `No. 004`<br>⑤ **全程 420ms** |
| **★ 节拍** | **252ms 走位 → 126ms 收束 → 42ms 高光**（60 / 30 / 10）。高光 = **印正**那一瞬：错位量归零、画面第一次"对上"。之后一切静止，不许有余韵。详见 §6.5.5 |
| **★ 效果名** | **错版换版（Misregistration Swap）** |
| **★ 核心约束** | **纸不动，动的只有墨。**<br>背景层 / 纸纹颗粒层全程零位移、零变化。位移只发生在图像的墨色层。<br>这一条决定了它是「印刷语言」还是「网页特效」——前者别人抄不走，因为它长在作品里。 |
| **可打断** | 切换动画进行中再次切换 → **立即接管**，从当前视觉状态续接新动画，不排队、不卡顿、不出现空白帧 |
| **禁止** | ❌ 白屏或黑闪过渡 ❌ 缩放（zoom）转场 ❌ 3D 翻转 ❌ 任何与「印刷」无关的转场 |
| **静置态（吸收自 research 5.3）** | 在展场网格中，鼠标悬停任一卡片 → 其**错版层分离 4–6px**，移开后在 200ms 内合回原位。移动端不适用。 |

**为什么选错版而不是翻页/缩放**：
> 丝网印刷的语言本来就是**墨色错位**——两张版没对准，红色往外偏一毫米。它是静态的、纸上的、属于印刷的物理缺陷。
> 让它在切换时**真的错位、再合回去**，炫的就不是外挂特效，是**这张卡本身的性质**。
> 观众看到的不是「这个网页会动」，是「原来错版是可以活的」。

### 5.4 F-04 · 展场（The Floor）｜必要支撑

> 作者指定项（F-02、F-03）需要载体：没有「有图可切」「有主题可切」的容器，前两项无法成立。故列为必要支撑。

| 项 | 内容 |
|---|---|
| **用户动作** | 揭幕结束后即在此屏。上下滚动浏览；点击任意卡片 |
| **可观察结果** | ① 当前主题的全部作品以 2:3 竖版卡片网格呈现，**图不裁切、不变形**<br>② 每张卡片下方只有：作品名 + `No. 00X` 两行，无旁白、无年份、无说明文字<br>③ 滚动全程卡片依次浮现（每次进入视口时轻微上浮出现）<br>④ 悬停卡片 → 错版层分离（§5.3）<br>⑤ 点击卡片 → 进入聚焦视图 |
| **顶部常驻** | 站名（小字）+ 系列名列表（= 主题切换器） |
| **约束** | 页面上任何时刻，**作品的视觉重量必须大于一切 UI** |

### 5.5 F-05 · 聚焦（Focus）｜必要支撑

| 项 | 内容 |
|---|---|
| **用户动作** | 在展场点击一张卡片 / 在聚焦视图按 `Esc` 或点关闭 |
| **可观察结果** | ① 进入：卡片从网格原位**放大并移至画面中央**（不为叠一层黑色遮罩 + 突然弹图）<br>② 画面中央为该作品的 2:3 大图，右侧或下方为三行文字：`作品名` / `系列名` / `No. 00X / 0NN`<br>③ 左右箭头 + 键盘可切换（§5.3）<br>④ 关闭：反向动画回到网格中它原来的位置 |
| **约束** | 电脑上按 `←` `→` 与点击箭头的效果必须完全一致 |

---

## 六、视觉规格

> **v2 重写 · v3 收口 · 2026-09-19**——作者追加要求：「用我的审美知识来设计，要高级感和设计感，一眼就看出和别人的不一样，**违背约束也可以，但是不可以违和**」，随后指令「**还是用 60-30-10 面积配比**」。
> 本节据此重写。依据作者审美知识库六张卡：`风格卡-极繁艺术` ／ `知识卡-印刷-印刷工艺语言` ／ `知识卡-展陈-博物馆式观看节奏` ／ `案例卡-时装设计-极繁三色纪律-绯金折枝` ／ `知识卡-构成-重复与变异` ／ `知识卡-构图-动势与张力`。

### 6.0 设计公理：守配比，守关系，只在金属上破

**先给「违和」一个物理定义。** 它不是「色彩太大胆」，而是下面两件事之一：

| 违和的两个物理来源 | 后果 | 依据 |
|---|---|---|
| **亮度比失衡** —— **两块大面积色域**的明度反差超过 3:1 |
| **层级缺失** —— 所有元素平分权重，没有主角 | 崩塌成噪音 | 极繁卡：等级而非平等，主次必须清晰 |

**所以：配比可以调，关系不能破。而 v3 的结论更进一步——连配比也不必调：60-30-10 守住，只在「饱和度」这一项上开一道窄口，留给金属。**

| | v1 原约束 | v3 处置（**已收口**） |
|---|---|---|
| 1 | 严格 60-30-10 面积配比 | ✅ **守** —— 作者 2026-09-19 指令「还是用 60-30-10 面积配比」。**v2 的「+5」已收回**（见 6.1），并**扩用到时间轴**（见 6.5） |
| 2 | 保持纸感：低饱和、无高光、无渐变光泽 | ⚠️ **只在金属上破** —— 全站唯一允许高饱和的地方是**金属专版**。纸与文字保持低饱和 |
| 3 | 强调色面积 ≤ 10% | ✅ **守** —— 金属专版就是那 10%，职责由「强调」升级为「黏合」 |
| 4 | 正文对比度 ≥ 4.5:1 | ✅ **守**（可读性不是审美问题）——这条是**下限**，管**文字**；A 条的 3:1 是**上限**，管**大面积色域**。两者方向相反但互不冲突 |

| | v2 新约束 | 依据 |
|---|---|---|
| **A** | **色域之间的亮度比 ≤ 3:1** —— 指**两块大面积色域**（主版 vs 辅版）之间的关系。**文字与背景另由「对比度 ≥ 4.5:1」约束**（见第 4 条）。一条是上限、一条是下限，作用对象不同，**不可混谈** | 展陈卡：「超过就会显得刺眼、廉价」 |
| **B** | **三级形状层级**：大形（卡片）／中形（版号·系列名）／小形（对位线·网点）——**三级绝不叠在同一块区域** | 极繁卡 + 绯金折枝：「繁而不乱」的唯一硬规矩 |
| **C** | **一墙一主角**：每屏只有一个视觉核心，其余全是次焦点与点缀，绝不平分权重 | 展陈卡 |
| **D** | **金属只做黏合**：只出现在边界、线脚、编号上，**不做填充** | 极繁卡：「作用不是装饰，是焊住冲突」 |
| **E** | **约束先写死，再动笔** | 绯金折枝：「极繁最大的失败模式是什么都想放，必须先关掉入口」 |
| **F** | ★ **60-30-10 是双轴的**：空间轴管「颜色面积」，时间轴管「动效预算」——同一把尺子量两个维度 | 作者 2026-09-19 指令；详见 §6.5 |

★ **第 D 条就是「违背约束但不违和」的全部机制。**
画面越浓，越需要一个人来调解。金属色在丝网印刷里是一块**真实的专色版**——专色能覆盖 CMYK 之外的金、银、荧光，它的物理职责就是「盖」，不是「混」。它把两组本来冲突的颜色焊在一起。

**这不是我在炫，是工艺本来就这么干。**

**★ 与作者审美观第三条的校准**

作者审美观第三条是：「审美不只是好看，更是**好用**——真正的高级审美是**隐形的**」。这看起来和「一眼看出不一样」打架，其实两者分工不同：

| | 显形的地方 | 隐形的地方 |
|---|---|---|
| **是什么** | **骨相**：构图、配色、质感、印刷物证 | **皮相**：导航、按钮、状态提示、说明文字 |
| **力度** | 大胆、强烈、第一眼就与别人不同 | 退到近乎不存在 |

**一句话：组织感要隐形，冲击力要显形。**
观众最后该记住的是「这个站像一张刚下机的印张」，不是「这个站有六个设计决策」。

### 6.1 主题系统：从「一套配色」改为「三块印版」

v1 的「一底色 + 一字色 + 一强调色」是网页配色法。**丝网印刷的真相是「一版一色」**：每种颜色要一块独立的网版。所以丝网的第一法则是**限色**——不是用颜色去填充造型，而是**用有限的版数逼出层次**。

每个系列因此不是「一套配色」，而是**三块要上机的版**——第四色不由制版产生：

| 版 | 角色 | 占比 | 说明 |
|---|---|---|---|
| **主版** | 基调 | 60% | 可以是浓色了，不再强制纸白 |
| **辅版** | 冲突与层次 | 30% | 与主版**叠印**会物理混色，自动长出第三色 |
| **金属专版** | **黏合剂** | 10% | 金／银／铜。只走编号、版号线、边框，**不做填充** |

**配比到此为止：60-30-10，三个数，没有第四个。**（作者 2026-09-19 指令）

> ⚠️ **本条是 v3 对 v2 的收回。** v2 曾写「**60-30-10+5**」，把「一点绿」当成第四色配比——那已经不是配比，是改配比。
> v3 明确：**60-30-10 就是全部配比**。原「点睛色」不再是一块要上机的版，改按下面的「**第 N+1 色**」处理——它是工艺副产品，不需要制版，因此不占配比。

**六套主题的印版规格（作者 2026-09-19 已定稿，见 §8 决策记录）：**

| 系列 | 主版 60% | 辅版 30% | 金属专版 10% | 第 N+1 色（不占配比） |
|---|---|---|---|---|
| `SLEIGHT` · Now You See Me | `#12100D` 幕黑 | `#8E2B3A` 暗绯 | `#C9962B` 金 | `#F0E9DC` 珠白 |
| `UNREAL` · 超现实主义 | `#2F5D8C` 群青 | `#EAE4D9` 旧纸 | `#B9BFC4` 银 | `#C8442E` 绯 |
| `LIMBO` · 盗梦空间 | `#1B2026` 钢灰 | `#7C9AAF` 雾蓝 | `#A8B4BD` 银 | `#D98B4A` 琥珀 |
| `WORLD LINE` · Steins;Gate | `#2A211C` 暗棕 | `#C4562F` 柿红 | `#B87333` 铜 | `#EDE4DA` 米白 |
| `FIFTY-TWO` · 扑克牌 | `#1C1B19` 墨黑 | `#FAF8F4` 纸白 | `#C9962B` 金 | `#B3242B` 正红 |
| `ARCANA` · 塔罗牌 | `#2B2A4A` 夜靛 | `#E8E3D5` 月白 | `#C9A227` 金 | `#4A6B52` 苔绿 |

> **三条跨主题纪律**
> ① 主版与辅版必须来自**不同语义**（一浓一淡 / 一冷一暖 / 一暗一明），否则叠印区做不出第三色。
> ② 金属专版**六套里必须都真用金属**（金 / 银 / 铜），不得用红、蓝之类的非金属色顶替——它是黏合剂，不是第四个点缀色。
> ③ **纸色（浅色）在本版统一不承担 60%。** 六套主题的主版**全部是浓色**——纸色退到辅版（`UNREAL` / `FIFTY-TWO` / `ARCANA`，靠叠印出中间调）或第 N+1 色（`SLEIGHT` / `LIMBO` / `WORLD LINE`，靠色相区分）。这是本版与 v1「纸白 60%」的根本差异。
>
> ★ **第四条（v3 新增）**：**第 5 列不是第四块版。** 它只有一个合法来源——**主版 × 辅版 叠印**混出的颜色，或**纸上没上墨处透出的纸色**（P6：纸色是第 N+1 色）。两者都由工艺自动生成、不需制版，所以不进 60-30-10。
> 若某个主题的第 5 列颜色**无法**由叠印或露白得到，那它是一个**错误**，必须改掉——**而不是给它加一块版**。（见 §8 Q8）

### 6.2 构图规则

**「页面需要有色彩和构图」的具体落实：**

| 项 | 规则 |
|---|---|
| **基本单元** | 2:3 竖版卡片。**任何情况下不裁切、不变形、不拉伸** |
| **列数** | 桌面 ≥1280px：4 列 ／ 平板 768–1279px：3 列 ／ 手机 <768px：2 列 |
| **卡片间距** | 按展陈的间隔公式算：`(可用宽 − 卡片宽之和) ÷ (列数 + 1)`。结果**不得小于卡片宽度的 5%**；小于说明元素太多，**减一列**，不是硬挤 |
| **页面留白** | 左右各为视口宽度的 8%（桌面）– 6%（手机） |
| **视觉重心** | 每张卡片的视觉重心落在**中线带**（中部偏上），不是几何正中——这是展陈「1.5 米中线」在平面上的翻译 |
| **对齐** | 同排卡片**顶部严格对齐**（2:3 统一比例下，等宽即等高） |
| **「被策展过」的感觉从哪来** | 不做静态错落（会破坏排列感）。改为：**主题切换时卡片错峰入位**（§5.2 ②③）——秩序感来自动作，不是来自歪斜 |
| **背景角色** | 背景是**纸**：承载纸色与网点（见 6.4 · P4）。**不参与叙事、不做焦点**。换主题 = **换版**，不是换色（§5.2） |
| **亮度比** | 两块**大面积色域**之间的明度反差 **≤ 3:1**（硬约束 A，见 6.0）；**文字对比度**另由 ≥ 4.5:1 约束（见 §6.3 / AC-46） |

### 6.3 文字与语言

| 项 | 规定 |
|---|---|
| **界面文案** | **一律英文**（作者指定）。展站的对外调性、以及参考站（Sheehy / Hobbs / Salvat / ignoredone）全部为英文标题体系 |
| **字体** | 本版不指定具体字体，留待 Day 5 技术选型。但须满足：① 支持英文 ② 系统自带或极轻量 ③ 无花体、无装饰 |
| **界面文案总预算** | **≤ 40 个英文单词，全站**（**作品名与系列名不计入**——那是内容）。超出即视为设计失控 |
| **各级字号** | 站名 ≫ 系列名 > 作品名 > 编号。层级靠字号与字重，不靠颜色 |
| **禁止** | ❌ 感叹号 ❌ 营销语（`Discover` / `Explore` / `Dive into`）❌ 说明性段落 ❌「建设中」之外的任何状态提示语 |

**关键文案定稿（可直接用于 Day 7）：**

| 位置 | 文案 |
|---|---|
| 字标 | `ART EXHIBITION` |
| 系列导航 | `SLEIGHT` `UNREAL` `LIMBO` `WORLD LINE` `FIFTY-TWO` `ARCANA` |
| 卡片副行 | `No. 001` … `No. 0NN` |
| 聚焦视图 | 作品名 / 系列名 / `No. 003 / 012` |
| 空态 | `IN PROGRESS` |

### 6.4 印刷物证：让它是「一张印张」，不是「一个网页」

这一节直接回答「一眼就看出和别人的不一样」。

同行的作品站，都是**网页设计师做的网页**。本版把界面本身当作**一张丝网印刷品**来做——所以差异不是风格级的，是**物种级**的。

| # | 物证 | 具体规格 | 为什么它成立 |
|---|---|---|---|
| **P1** | **错版** Misregistration | 每块色版相对主版偏移 **2–4px**（对应纸质海报允许的 0.3–0.5mm 套准误差），方向固定，不随机 | 套准靠手工对位，层数越多偏得越多。**传统印刷当它是缺陷要报废，版画美学把它收编成签名** |
| **P2** | **十字规矩线** Register Marks | 页面四角的印张边线外，细十字 | 它是印刷厂的**原稿定位标记**。外行只觉得「专业」，同行会笑一下 |
| **P3** | **版号** Edition Mark | `No. 001`，金属专版色，字距拉开 | 呼应作者自己的判断：「红色印章和编号让作品有了**限量版画**的气质」 |
| **P4** | **网点底** Halftone | 纸面铺 **7px 间距、13% 墨量**的网点 | 丝网印不出真渐变，只能靠**半调网点**模拟——这是工艺的指纹 |
| **P5** | **85% 实地墨** | 大面积色块不用 100% 满版，降约 85%，留出纸的呼吸 | 印刷厂的真实做法：满版 100% 会打滑、卡纸、滚痕 |
| **P6** | **纸色是第 N+1 色** | 纸色本身参与混色，**不算在版数里** | 「选纸即选色」——纸不是背景，是一块免费的版 |

> **P1、P2、P4 是外行看得见、同行看得懂的三样东西。** 它们不增加装饰，只增加「这东西是真印出来的」的可信度。

---

### 6.5 动效规格：60-30-10 从「面积」扩到「时间」

> **v3 新增**。作者 2026-09-19 指令：「**动效和切换也要更有设计感**，还是用审美知识库 **60-30-10 面积配比**」。
> 依据四张卡：`知识卡-审美-60-30-10配色法则` ／ `知识卡-构成-重复与变异` ／ `知识卡-构图-动势与张力` ／ `知识卡-展陈-博物馆式观看节奏`。

#### 6.5.0 一句话

> **静态画面用 60-30-10 分配颜色，动起来的画面用同一把尺子分配注意力。**
> 而按下去的那一下，它的形态不是加速——**是停顿**。

**先立一条总则。**

**M0 · 一次交互只有一个高光。** 一屏之内不许有两处同时「发亮」。
（「一墙一主角」在时间轴上的翻译；也是「**埋一个特异点，且只埋一个**」。）

#### 6.5.1 双轴同构：60-30-10 量的是「谁占大头」

60-30-10 从来不是「配色法」，是**配比法**。配比管的是"谁占大头、谁只是点缀"。这个道理在两个轴上同样成立：

| 轴 | 60% | 30% | 10% |
|---|---|---|---|
| **空间 · 面积**（§6.1） | 主版色域 | 辅版色域 | 金属专版 |
| **时间 · 动的面积**（本节） | **静止区**（纸层、未参与元素） | **走位区**（位移、淡出淡入） | **事件区**（那一小块突变） |

**M1 · 动的面积 ≤ 30%。**
任何一帧，**正在改变的像素面积不超过全屏的 30%**。另 60% 必须**真的静止**；留给"事件"的只有一小块。

**例外（且全站只有这一个）：「全画幅工序」——换版与换印。** 这类工序按**时间轴**判定，不按单帧面积判定：三块版（或多层墨）**依次**到位，**任何时刻运动中的版本数量 ≤ 1**，且每一块自身的时长受 §6.5.5 约束。
除换版 / 换印之外的所有动效（卡片入位、悬停分离、字标显影），**一律按单帧面积判定**。

> ★ **这条让 §5.3 的「纸不动，动的只有墨」不再是特设纪律，而是配比法则的必然结果。**
> 纸层那 60% 之所以不能动，不是因为"我们规定它不能动"，而是因为**它已经占了 60%——占了 60% 的东西不许动。**

依据：
- 展陈卡「**一墙一主角**……只设一个磁石，其余做次焦点与点缀点，绝不平分权重」
- 动势卡「**背景尽量不动，倾斜才有参照**」
- 重复与变异卡「局部变化的比例不能过大，否则变异会从『亮点』变成『事故』」

#### 6.5.2 M2 · 高光是一次停顿，且只占 10% ★

**本节最重要的一条。**

按下去的那一下该长什么样？大多数网页动效的答案是**更快、更大、更多**。本版的答案是**不动**。

| | 常规做法 | 本版 |
|---|---|---|
| 高光帧 | 加速、放大、加光晕、抖动 | **其余一切停止，只有那一处完成最后一步** |
| 占比 | 越拖越长 | **≤ 总时长的 10%** |
| 之后 | 余韵、残影、粒子扩散 | **没有任何残余动作** |
| 观众感受 | "这个网页好炫" | "刚才那一下，好像真的印上去了" |

**M2 三条细则：**
1. 每次交互**只有一个**高光帧
2. 高光帧 **≤ 该动效总时长的 10%**（420ms 的动效 → ≤ 42ms）
3. **高光所在的那个元素，此后不再有任何动作。** 不许有余韵、残影、光晕扩散、粒子、缓收的尾巴——那一下之后，它就该是死的

> **高光落在哪一段？** F-02 与 F-03 都在**最末一下**；只有 **F-01 揭幕**把高光放在中段（合拢那一瞬），因为揭幕讲的是一个**事件**——版与纸对上，后面的收束是"开幕"这个后果。
> 位置可以不同，**但一次只有一下**，这一条不变。

依据：
- 展陈卡「缓冲带……**专属时刻**（每件作品拥有自己叙事的那一秒）」→ 在时间轴上，缓冲带就是这一顿
- 重复与变异卡「**把留白当休止符**……停顿是节奏的一部分，不是空隙」
- 重复与变异卡「**打破节奏，是最强的注意力信号**」→ 停顿，就是那个"打破"
- 展陈卡「疏可走马，密不透风」

> **一句话：华丽不来自加速，来自戛然而止。**

#### 6.5.3 M3 · 错峰不许均匀

> 「节奏 = 元素 + 间隔，**且间隔的权重更高**。元素决定『什么在跳』，**间隔决定『跳多快』**。间隔密则急促紧张，间隔疏则舒缓从容。**很多人只调元素、忽略间隔**——而节奏的快慢其实主要由间隔控制。」——《知识卡-构成-重复与变异》

所以卡片错峰**不能用等差**。等差是排队，是组件库的默认气质——也正是展陈卡判为「**均匀是最没节奏的节奏**」的那种东西。

| | 规则 |
|---|---|
| 形状 | **首尾疏、中段密**，首尾间隔约为中段的 **2 倍** |
| 依据 | 展陈「**20% 引入 / 60% 核心 / 20% 收束**」在时间轴上的翻译 |
| 禁止 | ❌ 全站所有动效共用一个错峰值　❌ 等差（0 / 60 / 120 / 180）　❌ 全部同步齐刷刷 |

> **这是全节最容易被忽略、也最能拉开差距的一条。** 均匀的错峰看起来"很整齐"——那正是网页默认款的脸。不均匀的错峰看起来"说不出的舒服"，因为那是**被策展过的节奏**。

#### 6.5.4 M4 · 运动曲线必须来自工序 ★

**这是「一眼看出不一样」在动效上的落点。**

网页动效的默认曲线（`ease` / `ease-in-out` / spring / bounce）都在模拟同一件事：**"自然的加减速"**。它模仿的是**手**。

但印刷不是手，是**机器**：**刮刀匀速走，压版压到底就停。**

只要把全站默认缓动换成「**匀速 + 急停**」，观感立刻不同——而且观众**说不出为什么**。

| 曲线 | 定义 | 用在哪 | 工序依据 |
|---|---|---|---|
| **刮墨** Squeegee | 匀速（linear） | 背景换版、网点显影、色块收拢 | 刮刀走速恒定 |
| **压印** Impression | 快起、急停，不回弹 | 字标显影、金属版落下 | 版压到纸面即停 |
| **对位** Register | 过冲后回正，**只回一次** | 错版合拢归零 | 手工对位总是先过一点、再退回 |
| **揭纸** Lift | 慢起、快走 | 旧墨移出、旧卡退出 | 揭纸要先把边角撬起来 |

**禁止**：❌ `ease` ❌ `ease-in-out` ❌ 弹簧 / bounce ❌ 任何回弹超过一次 ❌ 同一元素同时使用两种以上曲线

> **「对位」是全场唯一允许回弹的地方，且只允许一次**——这是动势卡的判词「**倾斜是一次性手段，一张图只用一两处**」在时间轴上的翻译。

#### 6.5.5 三条主线各自的节拍谱

时长不变（仍守 §5.1 的 1200ms、§5.2 的 600ms、§5.3 的 420ms），但**每一段都要标出它占 60 / 30 / 10 里的哪一份**。

**F-01 揭幕 · 1200ms**

| 段 | 时长 | 占比 | 内容 | 曲线 |
|---|---|---|---|---|
| 走位 | 720ms | 60% | 两道错位色块匀速向中线收拢 | 刮墨 |
| **高光** | **120ms** | **10%** | ★ 合拢那一瞬，字标自缝中**压印显影** | 压印 |
| 收束 | 360ms | 30% | 字标上移 + 展场自下而上浮现 + 卡片错峰入位 | 揭纸 |

**F-02 主题换版 · 600ms**（三块版依次过机，金属最后）

| 段 | 时长 | 占比 | 内容 | 曲线 |
|---|---|---|---|---|
| 走位 | 360ms | 60% | **主版**：新墨网点由稀到密压印，旧墨刮走 | 刮墨 |
| 收束 | 180ms | 30% | **辅版**：文字与线条换色、卡片错峰入位 | 揭纸 |
| **高光** | **60ms** | **10%** | ★ **金属专版**落下——激活态下划线亮起，全场唯一高饱和 | 压印 |

**F-03 错版换版 · 420ms**

| 段 | 时长 | 占比 | 内容 | 曲线 |
|---|---|---|---|---|
| 走位 | 252ms | 60% | 旧墨侧移 8px 淡出；新墨自反向 8px 移入 | 揭纸 / 压印 |
| 收束 | 126ms | 30% | 新墨归位，逼近主版 | 刮墨 |
| **高光** | **42ms** | **10%** | ★ **印正**——错位量归零「对上了」，画面第一次是正的 | 对位 |

> **高光都落在「合上」的那一下**（显影 / 落下 / 印正）——因为印刷的"华丽"只有一个动作：**版与纸对上**。
> **位置各有不同**：F-01 在**中段**（合拢即高光，余下 30% 是「开幕」这个后果的收束）；F-02 / F-03 在**最末一下**。位置可不同，**但一次只有一下**，这条不变。

#### 6.5.6 M5 · 与「减少动态效果」的关系

M0–M4 **全部让位于 AC-39**。开启系统 `prefers-reduced-motion` 后：

> 动效退化为「**只保留高光帧与终态，删掉全部走位**」。

即：一屏的 60%（走位）与 30%（收束）全部归零，只留下那 10%。
**这不是降级，是把本版的动效法则推到极限**——因为本版认为**高光本来就是精髓，走位只是搬运**。

---

## 七、后续（Backlog）｜【看情况加入】的效果

> 来源：`research.md` 6.2 中标注为「看情况」的全部条目。作者本次明确要求**列为后续，本期不做**。
> 每项保留了原文的触发条件——**只写「看情况」等于没写，必须写明什么条件下做。**

| # | 效果 | 做它的条件 | 不做的条件 |
|---|---|---|---|
| BL-1 | 全站页面转场 | 未来出现多页结构，且确有跨页跳转 | 保持单页结构（当前状态） |
| BL-2 | 毛玻璃 / 融球 / 螺旋扩散 | 某处背景确实空得撑不住，需要质地垫底——且叠加后作品的视觉重量仍占优 | 与 F-02 换版背景冲突时（当前状态） |
| BL-3 | 音波可视化 / 光标特效 | 只允许放在**非浏览区**（揭幕页、系列封面） | 出现在作品浏览区（当前状态） |
| BL-4 | 引入 JS 动画库 / WebGL / CDN | 某个动效原生 CSS / SVG 确实做不出，且它值「断网打不开」这个代价 | 能原生解决就绝不引（当前状态） |
| BL-5 | 搜索 / 筛选 | 作品规模增长到单屏扫不完（经验值：单系列 > 40 张） | 当前规模（6 系列） |
| BL-6 | 关于页 / 作者简介 | 有真实的对外合作需求，需要一段正式自我介绍 | 当前阶段 |
| BL-7 | 移动端专属动效 | 移动端访问占比成为主要来源 | 当前阶段 |
| BL-8 | 手工维护 → 轻量内容管理 | 作品更新频率高到手改数据成为负担 | 当前频率（季度级） |

> **统一红线（不因「后续」而松动）**：任何动效，只要它让观众第一眼看的不是卡片，就砍掉。
> 条件是「加不加」，红线是「加了之后要不要撤」。

---

## 八、决策记录（Decision Log）

> 作者 2026-09-19 指令：「**都按推荐的来**」。Q1–Q9 **全部按建议采纳**，本节由「待确认」转为**已定稿记录**——Day 5 / Day 7 直接依此执行，不再回问。

| # | 决策 | 结论 | 落实位置 |
|---|---|---|---|
| Q1 | F-03 吸收 hover 错版分离 | ✅ **采纳** —— 两者是同一套印刷语言的两面，拆开要写两遍规则 | §5.3 静置态 |
| Q2 | 主题 = 系列（一套控件干两件事） | ✅ **采纳** —— 避免出现两组导航 | §5.0 / §5.2 |
| Q3 | 6 个系列英文名 | ✅ **采纳**：`SLEIGHT` / `UNREAL` / `LIMBO` / `WORLD LINE` / `FIFTY-TWO` / `ARCANA` | §6.1 表 / §6.3 |
| Q4 | 6 套主题印版规格（三版，**无第四色**） | ✅ **采纳** —— v1 的「一底色 + 一强调色」作废 | §6.1 表 |
| Q5 | 界面全英文（含系列名、作品编号） | ✅ **采纳** —— 文档本身仍为中文 | §6.3 / AC-31 |
| Q6 | 文件名大小写 | ✅ **定规**：**正式文档**用「首字母大写的专名」（`PRD.md`），**代码与资源**一律小写（`research.md` 不改名，避免断链）。有规则即不叫混用 | 仓库规范 |
| Q7 | v2「破 60-30-10」是否认可 | ❌ **否决** → ✅ v3 已收回，改为「守配比，并把它扩到时间轴」 | §6.0 / §6.1 / §6.5 |
| **Q8** | 第 N+1 色的合法性（§6.1 第四条） | ✅ **采纳** —— 只能由**主辅叠印**或**露白**产生，不占配比；**生成不了就改色，不加版** | §6.1 第四条 / AC-54 |
| **Q9** | 动效曲线：用「匀速 + 急停」取代网页默认缓动 | ✅ **采纳** —— 全站禁用 `ease` / `ease-in-out` / 弹簧回弹。这是本版动效「一眼看出不一样」的主要来源 | §6.5.4 / AC-60 |

---

## 九、下一步

| 步骤 | 产出 | 前置 |
|---|---|---|
| Day 5 | 技术选型（单文件？框架？动效方案原生与否） | 本 PRD 定稿 |
| Day 7 | MVP 实现 | Day 5 选型 + 本 PRD §10 验收标准 |
| Day 8+ | 按 §7 触发条件逐项翻案 | 用起来之后 |

**交接给 Day 7 的一句提醒**：

> §10 的 **74 条**不是「最好都满足」，是**验收清单**。实现完成后，逐条勾，勾不满不算完成。
> 其中 **AC-17（纸不动，墨动）**、**AC-39（尊重减少动态效果）**、**AC-57（高光 ≤10%）**、**AC-59（错峰不均匀）** 一旦漏掉，这个站要么从「印刷语言」退回成「网页特效」，要么从「印刷品」退回成「组件库」。

---

## 十、验收标准（可逐条检查）

> 用法：逐条勾选。**任一条未通过，MVP 不算完成。**
> 判定方式一栏写的是「你怎么亲眼确认」，不依赖任何口头结论。

### 10.1 揭幕 F-01

| # | 验收标准 | 判定方式 |
|---|---|---|
| AC-01 | 打开页面，揭幕自动播放，**1.2 秒内**结束并露出展场 | 录屏，逐帧数时长 |
| AC-02 | 揭幕期间画面**只有**色块 / 字标 / 细线，无第 4 种元素 | 截图，肉眼清点 |
| AC-03 | 画面上**没有**进度条、百分比、转圈动画 | 截图 |
| AC-04 | 揭幕进行中任意点击页面，**立即**跳到终态，不播残余动画 | 在约 0.4s 时点击，观察是否直接进入展场 |
| AC-05 | 全程**无音效** | 观察浏览器标签页无发声图标 |
| AC-06 | 揭幕结束后**无需任何操作**即可开始滚动浏览 | 重载页面后只等待、不操作 |

### 10.2 主题切换 F-02

| # | 验收标准 | 判定方式 |
|---|---|---|
| AC-07 | 点击任一系列名，背景在 **600ms** 内完成**换版**（新墨以网点由稀到密压印、旧墨刮走），过程可见、**非瞬间跳变** | 录屏，逐帧看背景变化方式（必须是网点压印，不是平滑色相渐变） |
| AC-08 | 切换后，强调色（下划线 / 编号）**同步**变为新主题色 | 截图对比切换前后 |
| AC-09 | 卡片**先退出后入位**、且**错峰**（不是齐刷刷一起动） | 录屏慢放 |
| AC-10 | 内容**没有**使用颜色渐变过渡（只有位移 + 淡出淡入） | 录屏，观察旧图是否"渐变成"新图 |
| AC-11 | **换版全程**任一帧，文字与背景对比度 ≥ 4.5:1（无读不清的瞬间） | 录屏逐帧截图，用对比度工具抽检 3 帧 |
| AC-12 | 切换后刷新页面，**仍停留在同一主题** | 刷新后截图 |
| AC-13 | 地址栏出现对应锚点（如 `#limbo`） | 看地址栏 |
| AC-14 | 6 个系列主题**逐个点一遍**，每个都能正确切换（无错色、无漏色） | 逐个点击截图 |
| AC-15 | **六套主题逐个**检查：文字与背景的对比度 ≥ 4.5:1，深主版上尤其不出现读不清的字 | 逐个主题截图，取色器算对比度 |

### 10.3 作品切换 F-03

| # | 验收标准 | 判定方式 |
|---|---|---|
| AC-16 | 在聚焦视图按 `→`，切换动效**总时长 420ms** | 录屏逐帧 |
| AC-17 | ★ **纸层全程零位移**：切换前后，背景与纸纹的位置**完全一致** | 截取切换前/中/后三帧背景区域，叠加比对 |
| AC-18 | 旧图墨色层向一侧移出并淡出，新图墨色层从反方向移入归位 | 录屏慢放 |
| AC-19 | 切换中的**每一帧**都不出现空白/纯黑/纯白画面 | 录屏逐帧检查 |
| AC-20 | 编号同步翻页（`No. 003` → `No. 004`） | 录屏 |
| AC-21 | 切换动画**进行中**再按一次 `→`，新动画立即接管，不卡顿、不排队、不跳到错误图片 | 快速连按 5 次，检查最终停在哪一张 |
| AC-22 | 点击右箭头 与 按 `→` 键，效果**完全一致** | 两种方式各做一次，录屏对比 |
| AC-23 | 在展场网格中悬停卡片，**错版层分离 4–6px**；移开后 **200ms 内**合回 | 录屏/截图 |
| AC-24 | 切换效果中**没有**缩放（zoom）或 3D 翻转 | 录屏 |
| AC-25 | 键盘 `←` 可正常切换上一张（不只是 `→`） | 操作验证 |

### 10.4 展场 F-04

| # | 验收标准 | 判定方式 |
|---|---|---|
| AC-26 | 卡片**严格保持 2:3 比例**，无裁切、无拉伸变形 | 截图，量卡片像素宽高比 = 1:1.5（±2%） |
| AC-27 | 桌面 1280px 以上宽屏：**4 列**；768–1279px：**3 列**；<768px：**2 列** | 三种宽度各截一张图 |
| AC-28 | 同排卡片**顶部严格对齐** | 截图 |
| AC-29 | 卡片文案**只有两行**：作品名 + `No. 00X`；无年份、无说明、无旁白 | 放大截图 |
| AC-30 | 滚动时卡片**依次浮现**（进入视口时轻微上浮出现） | 录屏 |
| AC-31 | **界面文案**总计 ≤ 40 个英文单词。**作品名与系列名不计入**（那是内容，不是界面文案） | 抄录全部界面文案（站名 / 导航 / 编号 / 空态）计数 |
| AC-32 | 页面**无**感叹号、无 `Explore` / `Discover` 类营销语 | 检查文案 |
| AC-33 | 任一时刻，作品的视觉重量大于所有 UI 元素（UI 不抢眼） | 请一位未见过此页的人看 3 秒，问他「第一眼看到什么」 |

### 10.5 聚焦 F-05

| # | 验收标准 | 判定方式 |
|---|---|---|
| AC-34 | 点击卡片进入聚焦时，从**网格原位**放大移入（不是黑底弹图） | 录屏 |
| AC-35 | 聚焦视图显示三行：作品名 / 系列名 / `No. 003 / 012` | 截图 |
| AC-36 | 按 `Esc` 关闭，反向动画回到网格中**它原来的位置** | 录屏 |
| AC-37 | 关闭后，展场滚动位置**保持不变**（不跳回顶部） | 关闭后截图对比 |

### 10.6 通用

| # | 验收标准 | 判定方式 |
|---|---|---|
| AC-38 | 断网状态下打开页面，**全部样式与动效正常工作**（无外部依赖） | 断网后硬刷新，录屏 |
| AC-39 | 开启系统「减少动态效果」（prefers-reduced-motion）后，动效退化为**只保留高光帧与终态**（删掉全部走位与收束，总时长 ≤ 120ms），页面仍完全可用 | 在系统/浏览器开启该选项后操作验证 |
| AC-40 | 目标浏览器（Chrome 最新版）中，滚动与悬停**无明显掉帧卡顿** | 目视 + 浏览器性能面板 |
| AC-41 | 无 404、无控制台报错 | 打开开发者工具查看 Console |
| AC-42 | 作品图片数量与内容正确，**无错图、无空缺** | 逐个系列翻完，清点 |

> **AC-17 与 AC-39 是本 PRD 里最重要、也最容易被实现者忽略的两条。** 前者守住「这是印刷不是特效」，后者守住「华丽不等于让人不适」。

### 10.7 视觉系统（v2 新增）

> 对应 §6.0–§6.4。这一组验收的，就是「一眼看出和别人不一样」到底有没有落地。

| # | 验收标准 | 判定方式 |
|---|---|---|
| AC-43 | 每个主题严格由**三块版**构成：主版 60% ／ 辅版 30% ／ 金属专版 10%。第 4 色若出现，必须是**叠印混色或露白**的产物，不占配比 | 截图后取色器统计三类版色占比；第 4 色能追溯到叠印区或纸面 |
| AC-44 | 金属专版色**只出现在编号、版号线、边框**上，**无任何大面积填充** | 截图肉眼确认 |
| AC-45 | 六套主题的金属专版**全部是真金属色**（金 ／ 银 ／ 铜），无红蓝等非金属色顶替 | 逐个主题截图 |
| AC-46 | ★ **两块大面积色域之间的明度反差 ≤ 3:1**。本条**只管色域**；文字与背景改由对比度 ≥ 4.5:1 约束（AC-11 / AC-15） | 取色器读主版色与辅版色，算明度比 |
| AC-47 | 卡片色版有**固定方向的 2–4px 错版偏移**（方向一致，不随机） | 放大截图量偏移量、比对方向 |
| AC-48 | 页面四角的印张边线**之外**，有细**十字规矩线** | 截图 |
| AC-49 | 纸面有**网点底**（约 7px 间隔 ／ 13% 墨量），放大后可见网点 | 放大到 400% 截图 |
| AC-50 | 大面积色块为 **≈85% 实地墨**，不是 100% 满版纯色 | 取色器读色值 |
| AC-51 | **三级形状层级**一眼可辨：大形（卡片）／中形（版号·系列名）／小形（对位线·网点），且**不叠在同一区域** | 缩到 25% 截图，看层级是否分得开 |
| AC-52 | 主题切换时**三块版依次到位**（不同时），且用**网点压印**而非平滑色相渐变 | 录屏逐帧 |
| AC-53 | 每屏**只有一个视觉核心**（卡片），没有第二个元素与之抢焦点 | 请一位没见过的人看 3 秒，问「第一眼看到什么」 |
| AC-54 | 叠印区自动生出第三色（**实际颜色数 > 声明版数**），且**不出现四个以上独立色相** | 取色器统计色相种类 |
| AC-55 | 换主题全程，**纸面网点与错版偏移保持不变**（换的是墨，不是纸） | 切换前后各截一帧，比对纸面区域 |

### 10.8 动效系统（v3 新增）

> 对应 §6.5。这一组验收的，是「动效**有没有设计感**」——不是「动得多不多」。

| # | 验收标准 | 判定方式 |
|---|---|---|
| AC-56 | 任一帧，**正在改变的像素面积 ≤ 全屏 30%**。**换版 / 换印类全画幅工序除外**——它们改按「**任一时刻运动中的版本数量 ≤ 1**」判定 | 普通动效：录屏逐帧圈出运动区域估面积；换版：录屏确认三块版不同时动 |
| AC-57 | ★ 每次交互**只有一个高光帧**，且 **≤ 该动效总时长的 10%** | 录屏逐帧，数高光出现次数与时长 |
| AC-58 | ★ **高光之后没有任何残余动作**（无余韵、无残影、无光晕扩散、无粒子） | 录屏——动效必须"戛然而止" |
| AC-59 | ★ 卡片入位错峰**非均匀**，首尾间隔约为中段的 **2 倍** | 录屏逐帧，量相邻卡片的入场时间差 |
| AC-60 | 全站**没有** `ease` / `ease-in-out` / 弹簧回弹曲线 | 代码检查（Day 7）＋ 录屏看有无"软着陆"感 |
| AC-61 | 背景换版 / 网点显影 / 色块收拢为**匀速**，无加减速 | 录屏逐帧，看相邻帧位移量是否相等 |
| AC-62 | 字标显影为**快起急停**，无回弹 | 录屏慢放 |
| AC-63 | 「对位」回弹（过冲后回正）**全站只出现在错版合拢一处，且只回一次** | 全站走一遍，录屏 |
| AC-64 | 任一元素**不同时使用两种以上运动曲线** | 代码检查（Day 7） |
| AC-65 | 三条主线实测时长：揭幕 **1200ms** ／ 主题换版 **600ms** ／ 错版换版 **420ms**（±5%） | 录屏逐帧 |
| AC-66 | 主题换版中，主版 → 辅版 → 金属专版**依次**到位，**金属最后** | 录屏 |
| AC-67 | 高光帧的对比突变只发生在**一小块区域（≤10% 屏面积）**，不出现全屏闪白 / 闪黑 | 录屏逐帧 |
| AC-68 | **快速连按 10 次**切换，错峰节奏不被破坏（不塌成齐刷刷） | 快速连按，录屏 |
| AC-69 | 开启「减少动态效果」后，**只保留高光帧与终态**，走位全部删除，页面仍完整可用 | 系统开关验证 |

### 10.9 自检补漏（v4 新增）

> 本节 5 条由 AI 自检发现——原稿在 §5.1 / §5.3 / §6.2 / §6.3 都写了规定，但**漏了对应验收**。

| # | 验收标准 | 判定方式 |
|---|---|---|
| AC-70 | **移动端横向滑动**可切换上一张 / 下一张，与箭头效果一致 | 手机实测，或 DevTools 触摸模拟 |
| AC-71 | 揭幕期间按**任意键**或**滚动**，同样立即跳到终态（不只是点击） | 在约 0.4s 时按空格 / 滚轮，观察是否直接进入展场 |
| AC-72 | 卡片间距 **≥ 卡片宽度的 5%**；不足时**自动减一列**，不硬挤 | 量相邻卡片间隙与卡片宽，算比值 |
| AC-73 | 页面左右留白 = 视口宽度 **8%**（≥1280px）／ **6%**（<768px） | 截图量留白像素 |
| AC-74 | 字号层级一眼可辨：**站名 ≫ 系列名 > 作品名 > 编号**（靠字号与字重，不靠颜色） | 截图比对四级字号 |

---

*最后更新：2026-09-19 · Day 4 · v4（自检后修订）*

