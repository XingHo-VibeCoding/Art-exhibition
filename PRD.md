# PRD.md — Art Exhibition 动态印刷剧场

> 试行版 · 2026-09-22
> 本文替换旧版静态展墙 PRD，定义 GSAP + Vite 迁移期间“做成什么样算完成”。

## 一、产品定位

一个把 2:3 竖版作品按系列组织起来的线上展览。网站不是作品缩略图目录，而是一台可观看的印刷机器：作品被压合、刷出、挂上墙、抽出成为校样，再回到原位。

目标用户在几秒内应该感受到三件事：作品是主角、系列有秩序、动效有物理来源。

## 二、本轮目标

1. 把当前原生 MVP 迁移到本地 `GSAP + Vite`；
2. 用统一时间轴取代零散 CSS 动画；
3. 让五种印刷动作贯穿入口、滚动、换版和聚焦；
4. 在桌面和移动端保持可操作、可读、可降级；
5. 每天都有可见证据，不用“应该完成”代替验收。

## 三、范围

### 必做

- 首屏揭幕：压、显影、收束、可跳过；
- 展墙滚动：主作、辅助作、版号分级进入；
- 主题换版：旧主题退出、新主题刷出、动画可接管；
- 聚焦校样台：原位放大、左右换印、关闭回位；
- 键盘、按钮、触摸拖拽和 reduced-motion；
- 根目录入口接入构建后的正式展览。

### 本轮不做

- 用户账号、商城、后台上传；
- 复杂评论社区和社交分享；
- WebGL 3D 展厅、实时粒子、声音系统；
- 无法解释物理来源的鼠标光效和无限循环动画。

## 四、观看流程

```text
揭幕 → 展墙 → 主题换版 → 滚动观看 → 点击作品 → 校样台
  ↑                                      ↓
  └────────────── 关闭并回到原位 ──────────┘
```

## 五、动效行为规格

### 5.1 揭幕

- 只能出现纸、墨、字标三类视觉层；
- 多层印版在 1.2 秒内压合，最后有一次短高光；
- 任意点击、按键或滚动可以跳到终态；
- 不得出现进度条、加载圈、白闪或黑闪。

### 5.2 展墙滚动

- 第一张主作先建立重量，辅助作品随后进入；
- 作品、卡纸和铭牌的入场延迟不同，但不能产生错位破坏；
- 进入视口后动画只播放一次；
- 不改变作品比例，不裁切、不变形、不造成横向溢出。

### 5.3 主题换版

- 主题按钮改变墙面、卡纸、专色和版号状态；
- 旧主题先退场，新主题由刷痕或遮蔽层揭出；
- 动画中再次选择主题时必须接管当前时间轴，不排队；
- 地址栏锚点和本地记忆继续工作；
- 六个主题都必须可切换，即使没有真实作品也要保持完整展位。

### 5.4 聚焦校样台

- 作品从网格原位进入中央校样台，优先使用 GSAP Flip；
- 校样台显示作品名、系列名和版号；
- 左右切换只改变换印过程，不制造通用卡片翻转；
- `Esc`、关闭按钮和触摸返回后回到原滚动位置。

## 六、内容与语言

- 页面界面文案使用英文，总量不超过 40 个英文单词；
- 作品名、系列名和版号属于内容，不计入界面文案；
- 代码注释、文档和协作说明使用中文；
- 作品素材全部本地化，不依赖远程图片或字体。

## 七、验收标准

### 功能

- `npm run build` 成功，无构建错误；
- 首屏、主题、滚动、聚焦四类流程均可从头走完；
- 键盘、按钮和触摸输入结果一致；
- 主题锚点刷新后保持；
- 聚焦关闭后滚动位置保持。

### 视觉

- 动效能对应压、刷、揭、对位、收束中的至少一种动作；
- 不使用 bounce、elastic、无限循环粒子或普通缩放转场；
- 桌面主作、辅助作和留白层级清晰；
- 所有文字在主题切换前后均可读；
- `prefers-reduced-motion` 下直接显示稳定终态。

### 证据

- 每日提供本地页面截图或交互过程证据；
- 视觉截图检查文件尺寸和体积；
- 明确记录已完成、部分完成、未完成和残余风险。

## 八、七日计划

| Day | 板块 | 完成标准 |
|---|---|---|
| 1 | Vite + GSAP 迁移 | 安装、启动、构建、旧数据可见 |
| 2 | 动效系统 | tokens、五种动作、降级与清理规则 |
| 3 | 首屏揭幕 | 时间轴、跳过、终态清理 |
| 4 | 展墙滚动 | ScrollTrigger、分级入场、性能 |
| 5 | 主题换版 | 刷痕时间轴、状态同步、接管 |
| 6 | 聚焦校样台 | Flip、换印、回位、拖拽 |
| 7 | 收口发布 | 根入口、移动端、性能、截图、GitHub |

## 九、决策记录

| 决策 | 结论 |
|---|---|
| 动画库 | 采用 GSAP，本地 npm 安装 |
| 构建工具 | 采用 Vite |
| UI 框架 | 不引入，保留原生 HTML/CSS/JS |
| 视觉方向 | 动态印刷剧场 |
| 旧方案 | 原生单文件作为迁移基线，不再作为未来技术指引 |
